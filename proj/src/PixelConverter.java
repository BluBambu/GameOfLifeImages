import org.newdawn.slick.Color;

public class PixelConverter {

    // TODO implement a more interesting conversion algorithm
    public boolean convertPixel(Color color) {

        return Math.random() > 0.997;
     //   return color.r > 0.1f;
    }
}
